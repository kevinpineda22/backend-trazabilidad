# 📊 ARQUITECTURA DEL SISTEMA DE APROBACIÓN

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                     SISTEMA DE APROBACIÓN CON TOKENS                         ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                              FLUJO COMPLETO                                  │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │   USUARIO CON   │
    │   AUTENTICACIÓN │
    └────────┬────────┘
             │
             │ 1. Accede a Gestión de Tokens
             ▼
    ┌────────────────────┐
    │  /gestion-tokens   │
    │                    │
    │  [Generar Token]   │
    │   ⚫ Empleado      │
    │   ⚫ Cliente       │
    │   ⚫ Proveedor     │
    └────────┬───────────┘
             │
             │ 2. POST /api/trazabilidad/tokens/generar
             ▼
    ┌────────────────────────────┐
    │   BASE DE DATOS SUPABASE   │
    │                            │
    │   tokens_registro          │
    │   ┌──────────────────┐     │
    │   │ id               │     │
    │   │ token (64 chars) │     │
    │   │ tipo             │     │
    │   │ expiracion       │     │
    │   │ usado: false     │     │
    │   └──────────────────┘     │
    └────────┬───────────────────┘
             │
             │ 3. Devuelve URL
             ▼
    ┌────────────────────────────┐
    │ https://tu-sitio.com/      │
    │ registro/empleado/abc123   │
    └────────┬───────────────────┘
             │
             │ 4. Usuario copia y comparte
             │    el link (email, WhatsApp)
             │
        ┌────┴─────┐
        │          │
        │  LINK    │
        │  📧📱    │
        │          │
        └────┬─────┘
             │
             │ 5. Persona externa accede
             │    (SIN necesidad de login)
             ▼
    ┌─────────────────────────┐
    │  /registro/:tipo/:token │
    │                         │
    │  [Valida Token]         │
    │  GET /tokens/validar    │
    └────────┬────────────────┘
             │
             │ Token válido?
       ┌─────┴─────┐
       │           │
      NO          SÍ
       │           │
       ▼           ▼
   ┌────────┐  ┌──────────────────┐
   │ Error  │  │  Muestra         │
   │ Token  │  │  Formulario      │
   │ Inváido│  │                  │
   └────────┘  │  [Nombre]        │
               │  [Apellidos]     │
               │  [Cédula]        │
               │  [Documentos]    │
               │                  │
               │  [Enviar] 🚀     │
               └────────┬─────────┘
                        │
                        │ 6. POST /registro-publico/:tipo/:token
                        ▼
               ┌────────────────────────┐
               │  registros_pendientes  │
               │                        │
               │  ┌──────────────────┐  │
               │  │ id               │  │
               │  │ tipo             │  │
               │  │ estado: PENDIENTE│  │
               │  │ datos (JSONB)    │  │
               │  │ token            │  │
               │  └──────────────────┘  │
               └────────┬───────────────┘
                        │
                        │ 7. Marca token como usado
                        ▼
               ┌────────────────────┐
               │  tokens_registro   │
               │  usado: true ✅    │
               └────────────────────┘
                        │
                        │ 8. Mensaje de éxito
                        ▼
               ┌────────────────────────┐
               │  ✅ Registro Enviado   │
               │                        │
               │  Su información será   │
               │  revisada y aprobada   │
               └────────────────────────┘


    ┌─────────────────┐
    │   USUARIO CON   │
    │   AUTENTICACIÓN │
    └────────┬────────┘
             │
             │ 9. Accede a Panel de Aprobaciones
             ▼
    ┌──────────────────────────┐
    │   /aprobaciones          │
    │                          │
    │   Tab: PENDIENTES        │
    │                          │
    │   ┌──────────────────┐   │
    │   │ 👤 Juan Pérez    │   │
    │   │ Empleado         │   │
    │   │ CC: 123456789    │   │
    │   │                  │   │
    │   │ 📄 Documentos:   │   │
    │   │ • Hoja de Vida   │   │
    │   │ • Cédula         │   │
    │   │ • Cert. Bancario │   │
    │   │                  │   │
    │   │ [✅ Aprobar]     │   │
    │   │ [❌ Rechazar]    │   │
    │   └──────────────────┘   │
    └──────────┬───────────────┘
               │
         ┌─────┴──────┐
         │            │
    ¿Aprobar o    Rechazar?
    Aprobar?          │
         │            │
         ▼            ▼
    ┌────────┐   ┌──────────────┐
    │ POST   │   │ POST         │
    │ aprobar│   │ rechazar/:id │
    │ /:id   │   │              │
    └───┬────┘   │ Body:        │
        │        │ {motivo: ""} │
        │        └───┬──────────┘
        │            │
        ▼            ▼
    ┌───────────────────────────────┐
    │  registros_pendientes         │
    │                               │
    │  estado: APROBADO/RECHAZADO  │
    │  fecha_aprobacion/rechazo    │
    │  aprobado_por/rechazado_por  │
    └───────┬───────────────────────┘
            │
            │ SI APROBADO:
            │ 10. Crea registro en tabla principal
            ▼
    ┌────────────────────────────────┐
    │  empleados_contabilidad        │
    │  clientes_contabilidad         │
    │  proveedores_contabilidad      │
    │                                │
    │  ✅ REGISTRO CREADO            │
    └────────┬───────────────────────┘
             │
             │ 11. Aparece en SuperAdmin
             ▼
    ┌────────────────────────────────┐
    │   /admin (SuperAdminContab)    │
    │                                │
    │   👥 Lista de Empleados        │
    │   📋 Juan Pérez - APROBADO ✅  │
    └────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                           ESTRUCTURA DE ARCHIVOS                             │
└──────────────────────────────────────────────────────────────────────────────┘

Backend:
├── controllers/
│   ├── tokensController.js          ← Genera, valida, lista tokens
│   ├── aprobacionesController.js    ← Aprueba, rechaza, historial
│   └── registroPublicoController.js ← Recibe registros públicos
│
├── routes/
│   ├── tokensRoutes.js
│   ├── aprobacionesRoutes.js
│   └── registroPublicoRoutes.js
│
└── app.js                            ← Importa y usa las 3 nuevas rutas

Frontend:
├── GestionTokens.jsx                 ← Panel admin para generar tokens
├── PanelAprobaciones.jsx             ← Panel admin para aprobar/rechazar
└── RegistroPublico.jsx               ← Formulario público (sin login)

Base de Datos (Supabase):
├── tokens_registro                   ← Almacena tokens con validez
└── registros_pendientes              ← Almacena registros pre-aprobación


┌──────────────────────────────────────────────────────────────────────────────┐
│                            ESTADOS DEL SISTEMA                               │
└──────────────────────────────────────────────────────────────────────────────┘

TOKEN STATES:
┌─────────┐   ┌──────────┐   ┌──────────┐
│ ACTIVO  │──▶│  USADO   │   │ EXPIRADO │
│  🟢     │   │   🔴     │   │   ⚫     │
└─────────┘   └──────────┘   └──────────┘
  3 días       1 uso solo     > 3 días

REGISTRO STATES:
┌───────────┐   ┌──────────┐
│ PENDIENTE │──▶│ APROBADO │──▶ Se crea en tabla principal
│    ⏳     │   │    ✅    │
└─────┬─────┘   └──────────┘
      │
      └────────▶┌───────────┐
                │ RECHAZADO │
                │    ❌     │
                └───────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                          ENDPOINTS DISPONIBLES                               │
└──────────────────────────────────────────────────────────────────────────────┘

🔐 PROTEGIDOS (requieren token JWT):
  POST   /api/trazabilidad/tokens/generar
  GET    /api/trazabilidad/tokens/listar
  GET    /api/trazabilidad/aprobaciones/pendientes
  POST   /api/trazabilidad/aprobaciones/aprobar/:id
  POST   /api/trazabilidad/aprobaciones/rechazar/:id
  GET    /api/trazabilidad/aprobaciones/historial

🌐 PÚBLICOS (sin autenticación):
  GET    /api/trazabilidad/tokens/validar/:token
  POST   /api/trazabilidad/registro-publico/empleado/:token
  POST   /api/trazabilidad/registro-publico/cliente/:token
  POST   /api/trazabilidad/registro-publico/proveedor/:token


┌──────────────────────────────────────────────────────────────────────────────┐
│                           SEGURIDAD Y VALIDACIÓN                             │
└──────────────────────────────────────────────────────────────────────────────┘

✅ Tokens únicos de 64 caracteres (imposibles de adivinar)
✅ Expiración automática después de 3 días
✅ Un solo uso por token (se marca como usado)
✅ Validación de tipo (empleado/cliente/proveedor)
✅ RLS activado en Supabase
✅ Rutas públicas separadas de las protegidas
✅ Validación de datos en backend
✅ Almacenamiento seguro en JSONB


┌──────────────────────────────────────────────────────────────────────────────┐
│                              VENTAJAS DEL SISTEMA                            │
└──────────────────────────────────────────────────────────────────────────────┘

✨ Control Total
   └─ Ningún registro se crea sin aprobación

✨ Seguridad
   └─ Tokens temporales y de un solo uso

✨ Trazabilidad
   └─ Historial completo de aprobaciones/rechazos

✨ Flexibilidad
   └─ Múltiples tokens activos simultáneamente

✨ Experiencia de Usuario
   └─ Formularios públicos sin necesidad de cuenta

✨ Profesionalismo
   └─ Proceso formal de onboarding


┌──────────────────────────────────────────────────────────────────────────────┐
│                                  RESUMEN                                     │
└──────────────────────────────────────────────────────────────────────────────┘

ANTES:
  CreacionEmpleado.jsx → empleados_contabilidad ✅

AHORA:
  GestionTokens → Token → RegistroPublico → registros_pendientes
  → PanelAprobaciones → Aprobar → empleados_contabilidad ✅

RESULTADO:
  Mayor control, seguridad y profesionalismo en el proceso de registro.
```
